# ===========================================
# 環境變數設定範本 (團隊共用)
# ===========================================
# 📖 使用說明:
# 1. 複製此檔案並重新命名為 .env
#    Windows PowerShell: Copy-Item .env.example .env
#    macOS/Linux:        cp .env.example .env
#
# 2. 填入實際的 API Key (需自行申請 Gemini API Key)
#
# 3. ⚠️ 不要將 .env 提交到 Git (已在 .gitignore)
# ===========================================

# -------------------------------------------
# AI API Keys
# -------------------------------------------
# Google Gemini API Key (請自行申請)
# 申請網址: https://aistudio.google.com/
# 當前使用模型: gemini-2.0-flash-lite
# 配額限制: 15 RPM, 1M TPM (免費額度)
LLM_API_KEY=your_gemini_api_key_here

# Groq API Key (備援用 - 可選)
# 獲取方式: https://console.groq.com/
GROQ_API_KEY=

# DeepSeek API Key (備援用 - 可選)
# 獲取方式: https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# -------------------------------------------
# Google Generative AI API 設定
# -------------------------------------------
# 強制使用 v1 API 端點 (不要修改)
GOOGLE_GENAI_API_VERSION=v1

# -------------------------------------------
# 資料庫設定 (Docker 模式)
# -------------------------------------------
# ⚠️ 重要: Docker 環境下必須使用服務名稱 "mysql"
#          不要改成 "localhost" 或 "127.0.0.1"
#          這些設定必須與 docker-compose.yml 一致

DB_HOST=mysql          # Docker 容器服務名稱
DB_PORT=3306          # MySQL 預設端口
DB_USER=root          # 資料庫使用者
DB_PASS=rootpassword  # 資料庫密碼
DB_NAME=outfit_db     # 資料庫名稱

# -------------------------------------------
# 跨平台設定指南
# -------------------------------------------
# 🪟 Windows 用戶:
#   - 使用 PowerShell 或 Git Bash
#   - 路徑使用正斜線 / 或雙反斜線 \\
#   - 確保 Docker Desktop 啟用 WSL2
#   - 檢查端口: netstat -ano | findstr "5001 3306 8080"
#
# 🍎 macOS 用戶:
#   - 使用 Terminal (zsh)
#   - Docker Desktop 分配 4GB+ 記憶體
#   - 檢查端口: lsof -i :5001 -i :3306 -i :8080
#
# 📝 兩個平台共通:
#   ✅ Docker Desktop 必須正在運行
#   ✅ 端口 5001, 3306, 8080 不能被佔用
#   ✅ 此檔案(.env.example)可提交,但 .env 不可
# -------------------------------------------
